/**
 * A model for the API docs model
 */
define(['backbone',
    'modules/apiDocumentation/resources/interfaces/interfaceCollection'
], function (Backbone, InterfaceCollection) {
    'use strict';

    return Backbone.Model.extend({
    });
});
