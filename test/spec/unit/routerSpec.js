define([
    'app'
], function (app) {
    'use strict';

    describe('Test router', function () {
        it('router should be defined', function () {
            expect(app).not.to.be.undefined;
        });
    });
});

