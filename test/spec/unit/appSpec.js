define([
    'app'
], function (app) {
    'use strict';

    describe('Test application', function () {
            it('application should be defined', function () {
                expect(app).not.to.be.undefined;
            });
    });
});
